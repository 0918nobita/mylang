{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "outputCapture": "std",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/language-server"
      ]
    }
  ]
}
